const MAX_FILE_MB = 2;
const MAX_FILE_SIZE = 1024 * 1024 * MAX_FILE_MB;
const ACCEPTED_IMAGE_MIME_TYPES = [
  "image/jpeg",
  "image/jpg",
  "image/png",
  "image/webp",
];

export { MAX_FILE_MB, MAX_FILE_SIZE, ACCEPTED_IMAGE_MIME_TYPES };
